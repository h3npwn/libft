# libft
Libft is my custom C library developed as part of the 42 curriculum. It re-implements standard C library functions and adds extra utilities for memory handling, string manipulation, and linked lists — all written from scratch in C.
